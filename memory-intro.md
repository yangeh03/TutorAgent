# DeepTutor Memory 设计说明

## 1. 文档目的

本文定义 DeepTutor 项目中 `Memory` 的统一设计口径，只描述设计，不描述实现细节。  
目标是把当前项目中已经存在但分散在不同子系统中的“记忆能力”整理成一套可解释、可扩展、可运营的架构说明，便于后续继续演进统一聊天、深度能力、TutorBot、多入口 API 与前端 Memory 工作台。

本文覆盖：

- 统一聊天体系中的会话级记忆
- 跨会话共享的长期记忆
- 深度能力内部的工作记忆
- TutorBot 的 agent-native 记忆与归档
- Memory 的存储边界、注入边界、更新边界和治理边界

本文不覆盖：

- 具体代码实现
- 具体模型提示词实现
- 具体数据库或文件操作细节
- 具体 UI 组件实现

---

## 2. 设计结论

DeepTutor 的 Memory 不应被定义为单一模块，而应被定义为一个四层记忆体系：

1. **工作记忆（Working Memory）**
   面向单次能力执行过程的临时推理空间，例如 `deep_solve` 中的 scratchpad、研究过程中的中间笔记、规划结果等。

2. **会话记忆（Session Memory）**
   面向单个 session 的上下文连续性记忆，包含消息历史、压缩摘要、turn 事件与局部偏好。

3. **共享长期记忆（Shared Long-term Memory）**
   面向用户维度、跨 session 生效的长期记忆，沉淀稳定画像与学习旅程。

4. **Agent 记忆（Bot-native Memory）**
   面向 TutorBot 运行时的自主归档、人格配置与上下文缩减机制。

这四层不是替代关系，而是职责分层关系：

- 工作记忆解决“本次任务怎么完成”
- 会话记忆解决“这次对话刚刚发生了什么”
- 长期记忆解决“这个用户长期是谁、最近在学什么”
- Agent 记忆解决“长期运行的 bot 如何在有限上下文中持续工作”

---

## 3. 设计目标

### 3.1 核心目标

- **连续性**：跨 turn、跨 session、跨入口保持用户体验连续。
- **个性化**：把稳定偏好、能力水平、学习背景沉淀为长期可用上下文。
- **轻量可控**：Memory 必须可读、可编辑、可清空、可刷新。
- **低耦合**：Memory 不能绑死某一个能力或某一个入口。
- **可解释**：用户与开发者都能直接看到“系统记住了什么”。
- **可扩展**：未来可以引入结构化索引、语义检索、掌握度模型，而不破坏现有接口。
- **共享但分权**：共享用户记忆与 bot 自身人格/工作区记忆必须分离。

### 3.2 设计原则

- **Markdown 作为共享长期记忆主视图**：长期记忆的主视图应保持人类可读、可手工修正。
- **SQLite 作为统一会话事实源**：会话级事实应以会话存储为准，长期记忆只做归纳，不做原始日志替代。
- **写入以归纳更新为主，不以原始堆积为主**：长期记忆应保留结论，不应无限追加对话原文。
- **注入要克制**：Memory 是辅助上下文，不应覆盖当前任务与当前问题。
- **人格与用户记忆分离**：`SOUL` 属于 bot，`PROFILE` / `SUMMARY` 属于用户。
- **源事实与派生摘要分离**：原始消息、会话压缩摘要、长期画像必须明确谁是源、谁是派生。

---

## 4. 非目标

当前阶段，Memory 设计不以以下能力为目标：

- 不把长期记忆设计成知识库或 RAG 的替代品
- 不把长期记忆设计成向量数据库优先系统
- 不把学习进度等同于“知识掌握度模型”
- 不把所有能力产物自动写入长期记忆
- 不要求长期记忆记录完整可追溯原始对话

如果未来需要语义检索、掌握度追踪、习题弱点画像，这些都应建立在现有分层 Memory 之上，而不是直接替换现有主视图。

---

## 5. 统一概念模型

### 5.1 工作记忆

工作记忆是能力内部的临时状态，用于支持单轮或单任务中的多阶段推理。

典型内容：

- 计划步骤
- 中间观察
- 工具调用结果摘要
- 引文收集
- 草稿与重规划信息

工作记忆的特点：

- 生命周期最短
- 作用范围最小
- 不直接作为用户长期画像
- 可在任务完成后部分提炼进入会话记忆或长期记忆

### 5.2 会话记忆

会话记忆是单个 session 的连续性载体。

典型内容：

- 用户与助手消息历史
- 历史压缩摘要
- 当前 turn 的事件流
- 当前会话下临时偏好与状态

会话记忆的特点：

- 来源于事实消息
- 受 token budget 约束
- 需要摘要压缩
- 主要服务于下一轮对话

### 5.3 共享长期记忆

共享长期记忆是面向用户的跨 session 持久化记忆，应在全功能对话与 TutorBot 间共享。

典型内容：

- 用户身份背景
- 学习偏好
- 知识水平
- 最近关注主题
- 已完成学习阶段
- 待解决问题

共享长期记忆的特点：

- 面向用户，不面向某个具体 bot
- 强调稳定性与可解释性
- 允许人工编辑纠偏
- 应被多入口统一消费

### 5.4 Agent 记忆

Agent 记忆是 TutorBot 长时运行所需的附加层，重点不在“记住所有内容”，而在“在有限上下文中保持工作能力”。

典型内容：

- 长期事实归纳
- 历史归档
- 人格配置
- 工作区内的本地回忆

Agent 记忆的特点：

- 强依赖上下文窗口管理
- 需要自动 consolidation
- 允许在无共享记忆时退化为工作区本地记忆

---

## 6. 分层架构设计

### 6.1 层次总览

| 层级 | 名称 | 作用范围 | 主体 | 生命周期 | 主存储形态 |
| --- | --- | --- | --- | --- | --- |
| L0 | Working Memory | 单次能力执行 | 能力实例 | 分钟级 | 内存对象 / 任务文件 |
| L1 | Session Memory | 单个 session | 用户会话 | 会话存续期 | SQLite / JSONL |
| L2 | Shared Long-term Memory | 跨 session、跨入口 | 用户 | 长期 | Markdown 文件 |
| L3 | Bot-native Memory | 单个 TutorBot 实例 | Bot | 长期 | 工作区文件 + 共享文件映射 |

### 6.2 层间关系

- L0 可读取 L1 与 L2，但不直接成为 L2。
- L1 是 L2 的事实来源之一，但不会自动全量提升为 L2。
- L2 为 L1 之后的每轮请求提供轻量背景。
- L3 可复用 L2，但不能与 L2 混淆。
- `SOUL`、技能、模板文件属于 L3，不属于 L2。

---

## 7. 存储设计

### 7.1 会话存储

统一聊天体系的会话层应以结构化会话存储为事实源，至少包含：

- `sessions`
  记录 session 元信息、压缩摘要、摘要边界、局部偏好。
- `messages`
  记录用户、助手、系统消息。
- `turns`
  记录每轮执行状态。
- `turn_events`
  记录流式事件、阶段事件、工具事件。

该层负责：

- 原始会话事实保留
- 上下文裁剪与压缩
- UI 历史回放
- 从最近 session 重建 Memory 刷新材料

### 7.2 长期共享存储

长期共享记忆应以 `data/memory/` 为统一公共根目录，作为用户级共享记忆空间。

该目录的规范角色应为：

- `PROFILE.md`
  稳定用户画像，记录身份、学习方式、知识水平、偏好等长期相对稳定信息。
- `SUMMARY.md`
  学习旅程摘要，记录当前焦点、近期完成项、待解决问题等动态但归纳后的内容。

设计上，这两个文件都应是**摘要文件**而不是原始日志文件。

### 7.3 Bot 工作区存储

TutorBot 工作区应继续保留本地文件能力，但职责需与共享记忆分离：

- `SOUL.md`
  bot 人格与行为原则，属于 bot 配置，而不是用户记忆。
- 本地 `MEMORY.md` / `HISTORY.md`
  只在未挂接共享长期记忆时作为降级方案存在。

规范上，不应把 `SOUL.md` 定义为共享用户记忆的一部分。

---

## 8. 规范文件语义

### 8.1 `PROFILE.md`

`PROFILE.md` 的设计目标是保存“稳定事实”。

应保存：

- 用户背景身份
- 学科领域或阶段
- 常用语言
- 偏好表达方式
- 学习风格
- 已知知识水平

不应保存：

- 一次性任务描述
- 某一轮即时提问
- 工具调用细节
- 已失效的阶段性状态

推荐语义结构：

- `Identity`
- `Learning Style`
- `Knowledge Level`
- `Preferences`

### 8.2 `SUMMARY.md`

`SUMMARY.md` 的设计目标是保存“阶段性学习状态摘要”。

应保存：

- 当前关注主题
- 最近完成的学习节点
- 未闭环的问题
- 最近显著变化

不应保存：

- 全量时间线原文
- 逐轮对话抄写
- 大量重复事实
- 纯临时噪声

推荐语义结构：

- `Current Focus`
- `Accomplishments`
- `Open Questions`

### 8.3 `SOUL.md`

`SOUL.md` 只描述 bot 自我设定，不描述用户。

应保存：

- 人格风格
- 沟通原则
- 教学哲学
- 行为约束

不应保存：

- 用户画像
- 用户学习历史
- 通用共享记忆

### 8.4 `HISTORY.md`

如果 TutorBot 需要 grep 友好的归档日志，设计上应使用独立的 `HISTORY.md` 或本地归档文件，而不应复用共享 `SUMMARY.md` 作为原始时间线。

这样可以避免“学习摘要”和“历史日志”语义混淆。

---

## 9. 读路径设计

### 9.1 统一聊天入口

统一聊天入口的读路径应为：

1. 从会话存储读取当前 session 的消息历史
2. 在 token budget 下构建最近历史与压缩摘要
3. 读取共享长期记忆
4. 把会话历史与长期记忆共同注入 `UnifiedContext`
5. 由具体 capability 决定如何消费

读路径原则：

- 会话记忆优先回答“刚刚聊了什么”
- 长期记忆补充回答“这个用户长期是谁”
- 长期记忆必须以系统背景的形式轻量注入

### 9.2 Chat 能力

`chat` 能力应完整消费 `memory_context`，把共享长期记忆作为背景系统上下文的一部分。

设计要求：

- Memory 为补充上下文，不覆盖 system prompt 本身
- Memory 不应改变工具启用策略，只影响回答风格与个性化关联
- 当 Memory 与当前问题无关时，模型应被引导忽略它

### 9.3 Deep Solve 能力

`deep_solve` 应区分两种记忆消费：

- **偏好级记忆**
  用于控制答案风格、详略、表达方式。
- **历史学习级记忆**
  用于避免重复解释用户已经掌握的基础，或在必要时补充相关历史背景。

设计上，`deep_solve` 对 Memory 的使用应比 `chat` 更克制，避免历史记忆污染当前严格求解过程。

### 9.4 Guided Learning / Research / Question

这些能力消费 Memory 时应按用途区分：

- `guide`
  更关注学习节奏、已学节点、用户理解风格。
- `research`
  更关注研究深度偏好、输出偏好，不应过度依赖长期记忆。
- `question`
  更关注知识水平与题目难度、题型偏好。

### 9.5 TutorBot

TutorBot 的系统提示构建应支持两种模式：

- **共享模式**
  直接消费共享 `PROFILE.md` 与 `SUMMARY.md`
- **本地模式**
  使用 bot 工作区自己的 `MEMORY.md` 与 `HISTORY.md`

设计上，共享模式应是标准模式，本地模式是降级模式。

---

## 10. 写路径设计

### 10.1 Turn 结束后的轻量刷新

每次统一聊天 turn 完成后，系统应从本轮用户输入与助手输出中判断是否需要更新共享长期记忆。

其设计语义是：

- 从最新 turn 提取长期有价值信息
- 与现有 `PROFILE` / `SUMMARY` 合并
- 如果没有新增有效信息，则保持不变

此过程的本质是**归纳更新**，不是日志追加。

### 10.2 基于 session 的重建刷新

系统应允许从某个 session 的近期会话重新刷新 Memory。

用途：

- 手工纠偏后重新归纳
- Memory 丢失后的再生成
- 对某一段关键学习会话做集中回填

设计语义：

- session 是事实源
- Memory 是派生产物
- Memory 可以被 session 重放重建

### 10.3 用户手工编辑

共享长期记忆必须支持用户直接编辑。

设计意义：

- 用户拥有最终解释权
- 当自动归纳不准确时可人工修正
- Memory 页面不只是只读展示，而是治理入口

手工编辑后的设计原则：

- 用户输入应被视为高优先级事实
- 后续自动刷新不应轻易覆盖人工明确设定的稳定偏好
- 自动刷新应更像增量修正，而不是把人工编辑全部改写掉

### 10.4 TutorBot 自动 consolidation

TutorBot 在上下文逼近窗口上限时，应触发历史 consolidation。

设计语义：

- 目标是释放上下文预算，而不是完整总结全部历史
- consolidation 边界必须与用户轮次对齐
- consolidation 后，旧消息不一定物理删除，但不再进入后续 prompt 主视图

### 10.5 失败降级

当自动 consolidation 或自动长期记忆更新失败时，系统需要有降级语义：

- 长期记忆更新失败时，不影响会话主流程
- TutorBot consolidation 多次失败时，可降级为原始归档
- 降级后的内容应与正式摘要区分开，避免污染共享长期记忆主视图

---

## 11. 注入策略设计

### 11.1 注入位置

共享长期记忆应通过统一上下文对象传入 capability，而不是每个能力自行读取磁盘。

这样做的设计收益：

- 统一入口控制
- 便于加预算控制
- 便于后续增加选择性注入、分层注入、AB 实验

### 11.2 注入预算

长期记忆必须有独立预算上限，避免无限拉长 prompt。

设计要求：

- `PROFILE` 与 `SUMMARY` 拼接后需受字符或 token 上限约束
- 截断应尽量保持结构完整
- 被截断时应明确这是摘要视图，而非全部 Memory

### 11.3 注入语气

Memory 应被标记为“背景记忆”而非“硬指令”。

这样可以避免：

- 覆盖主 system prompt
- 把过时记忆当作必须执行的命令
- 模型把记忆内容误解为当前用户问题

---

## 12. 共享与隔离设计

### 12.1 用户共享记忆

以下内容应在 DeepTutor 与 TutorBot 间共享：

- 用户画像
- 学习主题演进
- 当前学习重点
- 长期偏好

### 12.2 Bot 独占记忆

以下内容不应进入共享长期记忆：

- 某个 bot 的人格设定
- 某个 bot 的执行习惯
- 某个 bot 的工作区文件
- 某个 bot 的局部运行历史

### 12.3 共享边界规则

设计上应明确：

- `PROFILE.md` / `SUMMARY.md` 是用户维度
- `SOUL.md` 是 bot 维度
- 工作区 memory 是降级或局部 memory，不是公共用户 memory

---

## 13. 一致性与事实源设计

### 13.1 事实源优先级

事实源优先级应为：

1. 用户手工编辑的长期记忆
2. 原始 session 消息事实
3. 会话压缩摘要
4. 自动归纳生成的长期记忆

设计理由：

- 手工编辑代表用户明确校正
- 原始消息比摘要更接近事实
- 摘要与长期记忆都是派生层

### 13.2 幂等性

长期记忆刷新应具备幂等语义：

- 相同输入不应反复产生不同输出
- 无新增价值时应保持不变
- 长期记忆更新应以“是否变化”为显式结果

### 13.3 并发性

Memory 作为共享资源，设计上必须考虑：

- 多 turn 连续完成时的覆盖问题
- 用户手工编辑与自动刷新竞争问题
- 多 bot 并发写入共享用户记忆的问题

原则上：

- 会话层允许高并发
- 共享长期记忆层应表现为串行归纳视图

---

## 14. 当前项目中应统一的语义冲突

从现有项目结构看，Memory 已具备雏形，但存在几类需要统一设计口径的地方。

### 14.1 `SUMMARY` 的语义冲突

当前项目中，`SUMMARY` 同时被用于：

- 学习旅程摘要
- TutorBot 历史归档

这两个语义不应混用。

统一设计应规定：

- `SUMMARY.md` 只负责用户级学习摘要
- 需要 grep 的时间线历史时，使用独立 `HISTORY.md`

### 14.2 `SOUL` 的归属冲突

当前文档与子系统描述中，容易把 `SOUL` 误认为共享记忆的一部分。

统一设计应规定：

- `SOUL` 是 bot 配置
- `PROFILE` / `SUMMARY` 才是共享用户记忆

### 14.3 “共享”与“本地降级”并存

项目设计意图是让 DeepTutor 与 TutorBot 共享长期记忆，但 TutorBot 仍保留本地 memory 文件体系。

统一设计应规定：

- 共享模式是标准模式
- 本地模式是缺省共享不可用时的降级模式
- 两者的数据语义应兼容，但责任边界不同

### 14.4 `deep_solve` 的消费深度不足

当前 Memory 在 `chat` 里的角色更清晰，在 `deep_solve` 中主要偏向风格偏好，而对学习历史的消费较弱。

统一设计应规定：

- `deep_solve` 主要消费偏好记忆
- 是否消费学习历史应由任务类型决定
- 严格推理流程不能被长期记忆强绑定

### 14.5 学习进展与掌握度的概念混淆

当前项目已经保存了：

- session 历史
- guide 流程进度
- 长期摘要

但这不等于已经建成“掌握度模型”。

统一设计应明确：

- Memory 当前描述的是上下文连续性与学习旅程
- 不是知识点掌握度评估系统
- 如果未来扩展掌握度，应作为 L2.5 的结构化学习画像层新增，而不是塞进现有 `SUMMARY`

---

## 15. Memory API 设计角色

Memory API 的职责不应只是文件 CRUD，而应是 Memory 治理入口。

它至少应承担四种角色：

1. **读取**
   向前端展示当前长期记忆快照。

2. **手工治理**
   允许用户直接修正 `PROFILE` / `SUMMARY`。

3. **重建刷新**
   允许从某个 session 回放并重新生成长期记忆。

4. **清理**
   允许按文件或整体清空长期记忆。

设计上，API 读写的是**记忆快照视图**，不是所有底层事实源。

---

## 16. 前端 Memory 工作台设计

Memory 工作台的定位不是“高级设置页”，而是“记忆可视化与治理面板”。

它应满足：

- 双文档切换查看
- Markdown 直接编辑
- 最新更新时间展示
- 从当前活跃 session 刷新
- 对单文件执行清空
- 预览与编辑双视图

前端设计价值在于：

- 把自动记忆变成可见资产
- 允许用户纠偏
- 让长期记忆从黑箱变成白箱

---

## 17. TutorBot 记忆设计

### 17.1 目标

TutorBot 记忆的核心目标不是“做一个与统一聊天完全相同的 Memory”，而是：

- 支持长时间运行
- 避免上下文爆炸
- 保持人格稳定
- 在长期运行中不断提炼用户与任务事实

### 17.2 核心机制

TutorBot 的设计应包含：

- 历史消息 append-only
- 基于 token 压力的 consolidation
- 基于用户轮次边界的截断策略
- 长期事实更新
- 历史归档降级策略

### 17.3 与共享长期记忆的关系

TutorBot 应优先复用共享 `PROFILE` / `SUMMARY`，以避免：

- 同一用户在两个系统里形成两套画像
- 用户偏好在不同入口互相漂移
- bot 长期运行后与 DeepTutor 主产品认知脱节

同时，TutorBot 仍需保留：

- `SOUL`
- 本地工作区技能
- 本地历史归档

---

## 18. 演进路线设计

在不破坏当前项目结构的前提下，Memory 的演进应遵循以下顺序。

### 第一阶段：语义统一

- 统一 `PROFILE` / `SUMMARY` / `SOUL` / `HISTORY` 的文件职责
- 统一 DeepTutor 与 TutorBot 对共享长期记忆的定义
- 明确共享模式与本地降级模式

### 第二阶段：消费统一

- 所有 capability 统一通过 `UnifiedContext.memory_context` 消费长期记忆
- 区分偏好记忆与学习旅程记忆的消费策略

### 第三阶段：治理增强

- 保留手工编辑优先级
- 增加记忆变更可解释性
- 增加刷新来源与更新时间语义

### 第四阶段：结构化增强

- 在 Markdown 主视图之下引入结构化字段
- 支持结构化学习画像
- 支持掌握度、弱点、复习建议等派生层

### 第五阶段：检索增强

- 如有需要，为长期记忆建立派生索引
- 语义检索应是派生能力，而不是替代 Markdown 主视图

---

## 19. 最终设计口径

对 DeepTutor 项目而言，`Memory` 的最终标准定义应为：

> 一套分层的上下文连续性系统。  
> 其中，会话层保存事实与压缩摘要，长期层保存用户级画像与学习旅程，能力层保存临时工作状态，TutorBot 层负责长时运行下的归档与人格隔离。  
> 长期记忆以可读、可编辑、可共享的 Markdown 作为主视图，会话存储作为事实源，统一上下文作为注入通道。

换句话说，DeepTutor 的 Memory 不是一个“单文件功能”，也不是一个“向量数据库功能”，而是一个围绕用户连续学习体验构建的多层记忆架构。

