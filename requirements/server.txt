# ============================================
# DeepTutor Server — Full Dependencies (Web/API)
# ============================================
# Install: pip install -r requirements/server.txt && pip install -e .
# Everything needed to run the Python API server alongside the web frontend.

-r runtime.txt

# --- API server ---
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
