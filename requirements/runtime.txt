# ============================================
# DeepTutor Core Runtime Dependencies
# ============================================
# Install: pip install -r requirements/runtime.txt && pip install -e .
# Core Python runtime used by the Web/API build.

# --- Config & templating ---
python-dotenv>=1.0.0
PyYAML>=6.0
jinja2>=3.1.0

# --- LLM ---
openai>=1.30.0
tiktoken>=0.5.0

# --- LLM provider SDKs ---
anthropic>=0.30.0
dashscope>=1.14.0
perplexityai>=0.1.0

# --- HTTP clients ---
aiohttp>=3.9.4
httpx>=0.27.0
requests>=2.32.2

# --- Async helpers ---
nest_asyncio>=1.5.8
tenacity>=8.0.0

# --- Data models & storage ---
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiosqlite>=0.19.0

# --- RAG (LlamaIndex) ---
llama-index>=0.14.12
PyMuPDF>=1.26.0
numpy>=1.24.0,<2.0.0
arxiv>=2.0.0

# --- Web search ---
ddgs>=9.9.1

# --- Shared terminal rendering used outside the removed CLI package ---
rich>=13.0.0
